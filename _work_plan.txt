__WORK_PLAN__

??config选项中自定义要不要79空投
-- 更新碎墙美术资产
各个必杀
    -- 替罪羊
    -- 双手构
    -- 双手构开枪/蓄力开枪
    -- 双手构中段
    -- 双手构打击无敌下端
    -- 双手构全无敌击飞
    -- 链子
超必杀
    -- hz2626b
    -- tm6246改机械降神
    -- ha2424P
    -- hz24d6246d

-- 5Launcher_hold
-- knockout场景
-- 碎墙的血量伤害应用和碎墙knockout

-- 开始界面重做
-- 联机





-- 替罪羊pushbox逻辑
    获取两个角色的 collision_state
    local collision_state = table.concat({
        obj_char_LP["collision_move_available"][1],
        obj_char_LP["collision_move_available"][2],
        obj_char_RP["collision_move_available"][1],
        obj_char_RP["collision_move_available"][2]
    })
    生成位置：角色前一定距离 极限位置为板边
    生成时的碰撞盒挤开：
        1.从角色x y=0 替罪羊最高点最外点生成一个pushbox 如果碰撞则挤开 极限位置为板边
        2.

-- fuck this world coordinate axis
    -- just consider we are in a halo ring
    -- the ground of the ring at y = 0
    -- and so call up is toward the inside of the ring Y is negative
    -- the other side is Y positive