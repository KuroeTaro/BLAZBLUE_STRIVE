config选项中自定义要不要79空投

-- RC
-- 哈皮部分*各个必杀
-- 哈皮部分*超必杀
-- 第一次数值

-- 开始界面重做
-- 联机

-- 图片加载卡顿
-- fuck this world coordinate axis
    -- just consider we are in a halo ring
    -- the ground of the ring at y = 0
    -- and so call up is toward the inside of the ring Y is negative
    -- the other side is Y positive



-- 1. 碰撞系统的dx == 0的情况
2. 飞行道具的格式统一 hit类飞行道具的hurt_function统一 在main循环中添加飞行道具的friction gravity update
-- 3. side == L 和 side == R 都统一到 common_function
-- 4. 变量名修改

-- 修改TRM的air block
-- 修改蓝RC badge
-- 修改hit_side hurt_side 参数位置

-- 跳起后JD的方向输入保留延迟